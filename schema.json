{
  "type": "object",
  "properties": {
    "ForceEnableOnDomains": {
      "title": "Mandatory redaction domains",
      "description": "Hostnames on which text redaction is forced ON and cannot be turned off by the user. Subdomains are included. Accepts 'example.com', '.example.com' or '*.example.com'.",
      "type": "array",
      "items": { "type": "string" }
    }
  }
}
